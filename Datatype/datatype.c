#include<stdio.h>

int main() {

    // 1. Integer Data types: Used to store integer values.
    int a = 10;
    short b = 20;
    long c = 30;
    long long d = 40;
    signed int e = 50;
    unsigned int f = 60;

    printf("Integer Data types: \n");
    printf("int: %d\n", a);
    printf("short: %d\n", b);
    printf("long: %d\n", c);    
    printf("long long: %d\n", d);
    printf("signed int: %d\n", e);
    printf("unsigned int: %d\n", f);

    // 2. Floating Point Data types: Used to store decimal values.
    float g = 10.5;
    double h = 20.5;
    long double i = 30.5;
    printf("Floating Point Data types: \n");
    printf("float: %f\n", g);
    printf("double: %f\n", h);
    printf("long double: %f\n", i);

    // 3. Character Data types: Used to store a single character.
    char j = 'A';
    signed char k = 'B';
    unsigned char l = 'C';
    printf("Character Data types: \n");
    printf("char: %c\n", j);
    printf("signed char: %c\n", k);
    printf("unsigned char: %c\n", l);
   
    // 4. Void Data type: Used to specify the return type of functions that do not return a value.

    // 5. Boolean Data type: Used to store only two values, either true or false.

    // 6. Wide Character Data type: Used to store wide characters like Chinese, Japanese, Korean, etc.

    // 7. String Data type: Used to store a sequence of characters.

    // 8. Enumerated Data type: Used to define a set of named integer constants.

    // 9. User-defined Data type: Used to define a new data type using the struct keyword.

    // 10. Derived Data type: Used to create a new data type from the existing data types.

    return 0;
}