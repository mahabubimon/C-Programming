#include<stdio.h>

int main() {
    // ASCII value of a character, Number, Special character
    char character;
    printf("Enter any character: ");
    character = getchar();
    printf("ASCII value of %c = %d\n", character, character);

    

    return 0;
}